"""Seeknal Ask — NL-to-Analysis Agent.

Natural language interface for querying seeknal project artifacts.
Translates questions into DuckDB SQL, executes via the read-only REPL,
and returns data-backed answers.

Install: pip install seeknal[ask]
"""
