kind: profile
name: {{ name }}
description: "{{ description }}"
inputs:
  - ref: source.CHANGE_ME
# profile:
#   columns: []           # Optional: profile specific columns only
#   params:
#     max_top_values: 5   # Top N frequent values for string columns
