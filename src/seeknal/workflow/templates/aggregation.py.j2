# /// script
# requires-python = ">=3.11"
# dependencies = [
{% for dep in deps %}#     "{{ dep }}",
{% endfor %}
# ]
# ///
"""
Aggregation: {{ name }}

Generated by: seeknal draft aggregation {{ name }} --python
"""

from seeknal.pipeline import transform


@transform(
    name="{{ name }}",
    description="{{ description or 'Aggregation node' }}",
    # inputs=["feature_group.user_features"],  # Input feature group
)
def {{ name }}(ctx):
    """
    Aggregation function receives context with:
    - ctx.ref("kind.name") -> Get upstream DataFrame
    - ctx.duckdb -> DuckDB connection
    - ctx.config -> Profile configuration

    Return aggregated DataFrame.
    """
    df = ctx.ref("feature_group.user_features")

    # TODO: Implement aggregation logic
    raise NotImplementedError("TODO: Implement aggregation logic")
