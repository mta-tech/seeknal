# /// script
# requires-python = ">=3.11"
# dependencies = [
{% for dep in deps %}#     "{{ dep }}",
{% endfor %}
# ]
# ///
"""
Exposure: {{ name }}

Generated by: seeknal draft exposure {{ name }} --python
"""

from seeknal.pipeline import transform


@transform(
    name="{{ name }}",
    description="{{ description or 'Exposure definition' }}",
    # inputs=["model.predictions"],  # Model or output to expose
)
def {{ name }}(ctx):
    """
    Exposure function receives context with:
    - ctx.ref("kind.name") -> Get upstream DataFrame
    - ctx.duckdb -> DuckDB connection
    - ctx.config -> Profile configuration

    Return exposed DataFrame.
    """
    df = ctx.ref("model.predictions")

    # TODO: Implement exposure logic
    raise NotImplementedError("TODO: Implement exposure logic")
