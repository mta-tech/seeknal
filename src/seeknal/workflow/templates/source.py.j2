# /// script
# requires-python = ">=3.11"
# dependencies = [
{% for dep in deps %}#     "{{ dep }}",
{% endfor %}
# ]
# ///
"""
Source: {{ name }}

Generated by: seeknal draft source {{ name }} --python
"""

from seeknal.pipeline import source


@source(
    name="{{ name }}",
    source="csv",  # Options: csv, parquet, postgres, hive, etc.
    table="data/{{ name }}.csv",  # File path or table name
    description="{{ description or 'External data source' }}",
    # columns={"column1": "int", "column2": "str"},  # Optional column definitions
    # tags=["raw", "production"],  # Optional tags
)
def {{ name }}():
    """Source node for {{ name }}.

    Sources define external data inputs. The decorated function
    typically doesn't contain logic - data is loaded by the executor.
    """
    pass
