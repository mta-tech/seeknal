# /// script
# requires-python = ">=3.11"
# dependencies = [
{% for dep in deps %}#     "{{ dep }}",
{% endfor %}
# ]
# ///
"""
Rule: {{ name }}

Generated by: seeknal draft rule {{ name }} --python
"""

from seeknal.pipeline import transform


@transform(
    name="{{ name }}",
    description="{{ description or 'Validation rule' }}",
    # inputs=["feature_group.user_features"],  # Input to validate
)
def {{ name }}(ctx):
    """
    Rule function receives context with:
    - ctx.ref("kind.name") -> Get upstream DataFrame
    - ctx.duckdb -> DuckDB connection
    - ctx.config -> Profile configuration

    Return validation results.
    """
    df = ctx.ref("feature_group.user_features")

    # TODO: Implement rule logic
    raise NotImplementedError("TODO: Implement rule logic")
